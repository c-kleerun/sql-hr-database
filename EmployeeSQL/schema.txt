employees
--
emp_no PK INTEGER FK -< dept_emp.emp_no
emp_title INTEGER FK - titles.title_id
birth_date DATE
first_name VARCHAR
last_name VARCHAR
sex VARCHAR
hire_date DATE

dept_manager
--
dept_no PK INTEGER FK >-< dept_emp.dept_no
emp_no INTEGER

departments
--
dept_no PK INTEGER FK -< dept_manager.dept_no
dept_name VARCHAR

dept_emp
--
emp_no PK INTEGER
dept_no FK INTEGER

salaries
--
emp_no PK INTEGER FK - employees.emp_no
salary INTEGER

titles
--
title_id PK INTEGER
title VARCHAR